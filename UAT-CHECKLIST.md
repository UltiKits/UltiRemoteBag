# UltiRemoteBag — UAT Checklist

This document is the executable companion to `FEATURES.md`: one row per feature stating the
steps to exercise it and the observable truth that proves it works. It is an internal reference
for real-machine verification, not user-facing documentation.

> Batches are dispatched at 60 rows or fewer, and a batch never spans two repositories. There are
> exactly two legitimate exits to `human-uat-pending`: a row needing the pixel layer while the
> real-client harness is not ready, and a row needing personal credentials. Every other row must
> reach `pass`, `fail`, or `blocked`. This repository's meaningful rows generally need only ONE
> player, except the lock-arbitration rows (`ultiremotebag.admin.see-page.neg-owner-blocked-by-admin`,
> `ultiremotebag.admin.see.neg-readonly-then-owner-blocked` and the other `ultiremotebag.admin.see*`
> rows that name two accounts), which need a second account to act as the admin. That account holds
> `ultibag.use` and `ultibag.admin.see` and is NOT OP: no step in this file needs a test player to be
> OP (`/bag see` is gated by those two permission nodes only, and every framework or vanilla command a
> row names, such as `ul reload`, `give` or `data get`, is run from the server console).

## Conventions

- **Columns:** `ID`, `Preconditions`, `Steps`, `Expected`, `Layer`, `Covers`.
- **ID:** cites its `FEATURES.md` ID verbatim. A negative case suffixes the checklist ID only,
  as `.neg-<slug>` — a negative case still tests the same feature, so the base ID is unchanged.
- **Layer**, copied verbatim from Laojun's own `ultitools-real-client-uat` skill so no
  translation step exists at dispatch time: `protocol`, `java-client`, `os-input`, `pixel`,
  `server`, `human`.
- **Human-authenticated-session rows (D-27b):** a row whose Steps can only be exercised through
  the maintainer's own authenticated UltiCloud panel session carries the fixed Preconditions
  phrase `maintainer-authenticated UltiCloud panel session (personal credentials)` and Layer
  `human`. This repository has no such row — it has no panel-capability surface of its own — so
  none is currently affected; the convention is stated here for template consistency.
- **Expected** must name an observable truth — an exact chat line, a log line, a database row,
  an inventory slot — and never the words "it works".
- **Covers** back-references a Phase 9 GUI-excluded class name; left blank when no such class
  applies. `RemoteBagMainGUI` and `RemoteBagContentGUI` are this module's two entries in that
  register (`.planning/phases/09-module-ecosystem-readiness-and-test-coverage/gui-exclusions/
  UltiRemoteBag.md`), so every pixel-layer row exercising either GUI names it in this column.
- A row whose Preconditions name a prior row must appear after that row in file order — asserted
  mechanically: for every row, every checklist ID cited in its Preconditions cell must have a
  strictly smaller line number in this file than the row citing it (sweep class 8, D-27a).
- **Config-per-file rule (D-06):** one checklist row per `@ConfigEntity`-annotated class, never
  one row per key. This module ships exactly one `@ConfigEntity` (`RemoteBagConfig`,
  `config/remotebag.yml`), so exactly one config row exists below (ID suffixed `-yml`,
  `ultiremotebag.config.remotebag-yml`), aggregating every per-key
  `ultiremotebag.config.remotebag.*` row rather than citing a single one of them. This module
  ships no Maven-filtered (build-time) config file, so the build-time-property clause of this
  rule does not apply to any row below.
- This repository has no `config.yml` `language` key of its own — `plugin.i18n(...)` resolves
  through the framework's own global `language` setting (`zh`/`en`), and this module supports
  both (`UltiRemoteBag#supported` returns `["zh", "en"]`). Every row below whose Expected quotes a
  literal in-game line therefore carries the precondition `language: en` set in
  `plugins/UltiTools/config.yml`, matching the exact English text in `lang/en.yml` — **except**
  the read-only/blocked lock messages, which `BagOpenResult` builds directly with hardcoded
  Simplified Chinese text, ignoring the `language` setting entirely (a known product defect, see
  the two `.neg-owner-blocked-by-admin`/`.neg-readonly-then-owner-blocked` rows below, both citing
  the same filed issue, `UltiKits/UltiRemoteBag#20`). Those two rows quote the actual (Chinese)
  meaning in English prose rather than the literal characters, per this document's English-only
  rule; a real dispatch reads the exact characters from the cited source line rather than from
  this document.
- **Row order in this repository deliberately departs from strict ID-ascending** wherever strict
  alphabetical order would place a row citing another row's ID before that cited row (D-27a
  forbids this outright) — dependency order takes precedence over alphabetical order per feature
  section; alphabetical order is used only as the tiebreaker among rows with no such dependency.
- `plugin.i18n(...)` in this framework is a raw dictionary lookup with **no** `MessageFormat`
  processing — every Expected line below that quotes an i18n-templated message reproduces
  `lang/en.yml`'s literal placeholder substitution (`{0}`, `{1}`) rather than a re-derived phrase.

## Bag (player)

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiremotebag.bag.open | `language: en`; sender holds `ultibag.use` | Run `/bag` | `RemoteBagMainGUI` opens (see `ultiremotebag.bag.main-gui` below) | pixel | RemoteBagMainGUI |
| ultiremotebag.bag.open-page | `language: en`; sender holds `ultibag.use`; the sender owns at least page `1` (any fresh player does, per `RemoteBagService#getPlayerBagPages`'s empty-list fallback to page `1`) | Run `/bag 1` | `RemoteBagContentGUI` opens for page 1 in edit mode (see `ultiremotebag.bag.content-gui` below) — the lock is acquired for the sender as OWNER | pixel | RemoteBagContentGUI |
| ultiremotebag.bag.open-page.neg-out-of-range | `language: en`; sender's max page count is the shipped default 1 (fresh player, `permission_based_pages: true`, no `ultibag.pages.N` node held) | Run `/bag 2` | Chat line reads `Page 2 out of range, max page is 1` (red) — `BagCommand#openPage`'s range check refuses before attempting to open anything | server | |
| ultiremotebag.bag.open-page.neg-not-exist | `language: en`; `permission_based_pages: false` and `max_pages: 2` (raises the range to 2 without creating page 2 — with `permission_based_pages: false`, `getPlayerMaxPages` returns `max_pages` directly and never consults `default_pages` at all); sender is a fresh player who has never opened a bag GUI (existing pages therefore fall back to the singleton `[1]` per `RemoteBagService#getPlayerBagPages`) | Run `/bag 2` | Chat line reads `Bag #2 does not exist` (red, `bag_not_exist`) — `BagCommand#openPage`'s range check (1..2) passes, but the existence check fails because page 2 was never actually created | server | |
| ultiremotebag.bag.main-gui | `language: en`; `permission_based_pages: false` (NOT the shipped default — with it left at `true` and no `ultibag.pages.N` node granted, `getPlayerMaxPages` returns `default_pages` (1), and a fresh player's existing-page count is also 1, so `1 < 1` is false and the purchase icon never renders regardless of balance); with `permission_based_pages: false`, `getPlayerMaxPages` returns `max_pages` (10) directly, so the sender's one existing page is under the limit; `economy.price_increase_enabled: false` (NOT the shipped default — with it left at `true`, a fresh player's virtual page 1 makes the NEXT bag `nextBagNum = 2`, so `calculatePrice(2)` charges `ceil(10000 * 1.1) = 11000`, not the flat `economy.base_price`; disabling price increase keeps every purchase at the flat base price regardless of page number); Vault installed, `economy.enabled: true` (shipped default), and the sender's Vault balance is at least `economy.base_price` (10000, shipped default) | Run `/bag` | A 6-row inventory opens titled `<player> 's Remote Bags` (gold), listing exactly one chest icon (item/slot-usage lore) for the owned page, plus a GREEN minecart purchase icon (the balance precondition guarantees the affordable branch of `RemoteBagMainGUI#createPurchaseIcon` — a balance below the price would instead show a RED barrier icon, not exercised by this row); the previous/next navigation buttons plus a red `Close` button appear on the bottom row; the configured `sound.open` sound plays | pixel | RemoteBagMainGUI |
| ultiremotebag.bag.main-gui.slots-used-denominator | `language: en`; the sender owns page 1 and that page is FULL — all 45 content slots hold an item (fill it in `/bag 1` and click the page's own Save button, then close the page); `config/remotebag.yml` no longer contains `rows_per_page`, which a 6.3.0 install will not write; an upgraded file that still carries the line is fine for this row, because the key is gone from the code and the startup warning about it is exercised by its own row in the Lifecycle Hooks section | Run `/bag` and read the lore on page 1's chest icon | The lore line reads `Slots Used: 45/45`. Before `UltiKits/UltiRemoteBag#24` the denominator was `rows_per_page * 9`, so the same full page read `Slots Used: 45/54` at the shipped default and `Slots Used: 45/18` at `rows_per_page: 2` — in the first case promising nine slots that could never be filled, in the second reporting more used than the page was said to hold. Record `fail` for any denominator other than 45, including one that follows a `rows_per_page` line re-added to the file by hand: the key is gone from the code, so re-adding it must change nothing, and that is the cheapest way to prove the denominator is no longer reading it | pixel | RemoteBagMainGUI |
| ultiremotebag.bag.content-gui | `language: en`; the sender owns at least one bag page (run `ultiremotebag.bag.open-page` above first, or purchase one via `ultiremotebag.bag.main-gui` above); `sound.enabled: true` (shipped default) | Run `/bag <page>` for an owned, currently-unlocked page | A 6-row inventory opens titled `Bag #<page>` (gold), with any previously placed items visible in slots 0-44; the bottom row (slots 45-53) shows, in order: a back button (yellow, `Back`), two background fillers, a background filler (read-only-only refresh slot, hidden in edit mode), a green save button (`Save`), a green mode indicator (`Edit Mode`), two background fillers, and a red close button (`Close`); the configured `sound.open` sound plays | pixel | RemoteBagContentGUI |
| ultiremotebag.bag.content-gui.edit-item-movement | `language: en`; the sender owns a bag page opened in edit mode (run `ultiremotebag.bag.open-page` above); the sender's own inventory holds one `DIAMOND` on hotbar slot 0 and one `DIRT` on hotbar slot 3, and the page's content slots are empty | Inside the open page, in this order: (a) left-click the `DIAMOND` in the sender's own inventory to take it onto the cursor and left-click content slot 0 to place it; (b) left-click content slot 0 again to take it back onto the cursor and left-click it to put it down; (c) shift-click content slot 0; (d) left-click the `DIAMOND` where (c) put it to take it onto the cursor, left-click content slot 0 to put it back, then hover content slot 0 and press the number key for hotbar slot 3 (the `DIRT`). After each of (a)-(d), with the cursor empty, read the sender's own inventory as the server holds it (`minecraft:data get entity <player> Inventory`, which does NOT report a carried item). (e) Close the page with Escape (an edit-mode close saves content slots 0-44) and read the stored row: `sqlite3 plugins/UltiTools/sqliteDB/UltiRemoteBag.db "SELECT contents LIKE '%diamond%', contents LIKE '%dirt%' FROM remote_bags WHERE player_uuid='<uuid>' AND page_number=<page>"`. No step reads the page's own window, because this harness has no instrument for it: what the page holds is read from the stored row after the close | Every step takes effect and no step prints a refusal line. After (a) and after (b) the inventory read shows no `minecraft:diamond` entry and the `minecraft:dirt` at `3b`: the `DIAMOND` has left the sender's inventory while the cursor is empty, so it is in the page. After (c) the read shows exactly one `minecraft:diamond` entry, count 1, at whatever slot the shift-click chose, and the `DIRT` still at `3b`. After (d) the read shows the `minecraft:diamond` at `3b` and no `minecraft:dirt` entry. After (e) the query prints `0\|1`: the page stored the `DIRT` that (d)'s number-key swap put into content slot 0, and no `DIAMOND`. Before `UltiKits/UltiRemoteBag#27` every one of these except a shift-click INTO the page from the sender's own inventory was cancelled, which would have left both items in the sender's own inventory and the query printing `0\|0`, so this row is the positive counterpart of `ultiremotebag.admin.see.neg-readonly-then-owner-blocked` below and shares its instrument: the verdict is the item counts the server holds, never the absence of a chat line. `pass` requires every value above at once. Record `fail` if any step prints a refusal line or if the query prints `0\|0` (the page kept neither item, so a move was refused). Any other outcome, including an intermediate read that differs from the values above, records `blocked` with every read and the query output attached, never `pass`. Dragging is deliberately NOT a step here — a drag needs a stack and its own preconditions, and it is the half of `UltiKits/UltiRemoteBag#27` that had no real-machine coverage at all, so it has its own row: `ultiremotebag.bag.content-gui.edit-drag` below | server | RemoteBagContentGUI |
| ultiremotebag.bag.content-gui.edit-drag | `language: en`; the sender owns a bag page opened in edit mode (run `ultiremotebag.bag.open-page` above); the sender's own inventory holds a stack of EXACTLY TWO `DIAMOND`s on hotbar slot 0 and nothing else; the page's content slots are empty. The stack size is load-bearing: a one-item gesture is not a drag on a real server. Measured in `AbstractContainerMenu#doClick` (`paper-1.21.11.jar`): a hovered slot joins the quick-craft only while `carried.getCount() > quickcraftSlots.size()`, so with one item on the cursor the second slot is never registered, and a single registered slot is re-dispatched as a `ClickType.PICKUP` click with NO `InventoryDragEvent` constructed at all. Do not shrink the stack back to one — the row would then mark a correct build as failing, and would never reach the drag handler it exists to exercise | Inside the open page: left-click the two-`DIAMOND` stack onto the cursor, then left-drag across content slots 1 and 2 — press and hold the left mouse button, move the pointer over both slots, release. Then left-click an empty slot of your own inventory to put down whatever the cursor still holds, and read the sender's own inventory as the server holds it (`minecraft:data get entity <player> Inventory`, which does NOT report a carried item). Close the page with Escape (an edit-mode close saves content slots 0-44) and read the stored row: `sqlite3 plugins/UltiTools/sqliteDB/UltiRemoteBag.db "SELECT contents FROM remote_bags WHERE player_uuid='<uuid>' AND page_number=<page>"`. No step reads the page's own window, because this harness has no instrument for it: what the page holds is read from the stored row after the close | One `DIAMOND` is left in content slot 1 and one in content slot 2, and no refusal line is printed. The inventory read shows no `minecraft:diamond` entry: nothing was left on the cursor to put down, so both are in the page. The printed `contents` carries exactly two entries under `items`, keyed `'1'` and `'2'` (`RemoteBagService#serializeItems` writes one `items.<slot>` key per non-empty content slot), each with `id: minecraft:diamond` and `count: 1`, the form this server printed for a stored page on 2026-09-21. This is the positive half of `UltiKits/UltiRemoteBag#27`'s drag fix: `RemoteBagContentGUI#onDrag` was not overridden at all before it, so the GUI library's locked default cancelled every drag, in edit mode included. `pass` requires every value above at once. Record `fail` if a refusal line is printed, or if the inventory read shows both `DIAMOND`s back in the sender's inventory and nothing is stored (the drag was cancelled). Any other outcome, for example both `DIAMOND`s under one key or a key other than `'1'` and `'2'`, records `blocked` with the inventory read and the query output attached, never `pass` | server | RemoteBagContentGUI#onDrag |
| ultiremotebag.bag.content-gui.neg-toolbar-collect | `language: en`; the sender owns a bag page opened in edit mode (run `ultiremotebag.bag.open-page` above), whose content slot 0 holds one `DIAMOND` (place it as in `ultiremotebag.bag.content-gui.edit-item-movement` above) and whose content slot 1 is empty; the sender's own inventory is then EMPTY and is filled by exactly these two commands, in this order, so that the matching fixture lands on inventory slot `0b` and the control on `1b` (vanilla's `Inventory#add` fills the first free index and indices 0-8 are the hotbar) — first `/give <player> minecraft:gray_stained_glass_pane[minecraft:custom_name={text:"",extra:[{text:" "}]}] 1`, then `/give <player> minecraft:diamond 1`. **That `custom_name` is not a free choice, an anvil cannot produce it, and the obvious shorter spelling `[custom_name=" "]` does NOT match.** The chain that picks this spelling is measured rather than assumed, and what it predicts is a MEANING, never a printed form: `RemoteBagContentGUI#setupToolbar` fills bottom-row columns 1, 2, 3, 6 and 7 with `createBackgroundIcon()`, and `BaseInventoryPage#addToBottomRow` maps column `c` to raw slot `45 + c`, so in edit mode the filler icons are raw slots 46, 47, 48, 51 and 52; `BaseInventoryPage#createBackgroundIcon` (UltiTools-API 6.3.0-SNAPSHOT, this module's `provided` dependency) is `XVersionUtils.getColoredPlaneGlass(Colors.GRAY)` plus `icon.setName(" ")`, one U+0020 confirmed byte-for-byte with `od -c` rather than read off a tooltip; `Icon#setName` in `core-4.3.0.jar` is a bare `ItemMeta#setDisplayName(String)`; `CraftMetaItem#setDisplayName` stores `CraftChatMessage.fromStringOrNull(" ")`, and `CraftChatMessage$StringMessage` starts from `Component.empty()` and appends the typed text as a **sibling** (`appendNewComponent` builds `Component.literal(" ").setStyle(Style.EMPTY)` and appends it), so the stored component is a wrapper whose only child carries the space, and never a bare `" "`; and `ItemStack#isSimilar` resolves to `net.minecraft.world.item.ItemStack.isSameItemSameComponents`, whole-component equality, which is what makes the wrapper shape load-bearing — `[custom_name=" "]` yields an unwrapped `Component.literal(" ")`, fails that equality, and would make a correct guard look broken. The anvil route is impossible rather than merely awkward: `AnvilMenu#setItemName` runs the typed name through `validateName` and then, when `StringUtil.isBlank(name)` is true, **removes** `DataComponents.CUSTOM_NAME` instead of setting it, and a whitespace-only string is blank — so an anvil clears an item's name rather than setting it to a space. The first real-machine attempt at this row observed exactly that (a plain name worked as its positive control, a single space produced nothing) and could not be executed at all; do not restore the anvil step. All server-side measurements are read with `javap -c` from the Paper 1.21.11 jar this module is verified against (build 132). What the `/give` reproduces is a genuine production item: before `UltiKits/UltiRemoteBag#27` a filler icon could be taken out of a page, so a server upgrading from that version may have players holding real copies — the command is how a tester comes to hold the same item, not a claim about how a player obtains one. **No step reads the page's server-side window, and none needs to.** There are three instruments, all server-side: the sender's own inventory as the server holds it (`minecraft:data get entity <player> Inventory`), which does NOT report a carried (cursor) item, so every inventory read below is taken only after the carried stack has been put down into an empty slot of the sender's own inventory; the red chat lines the server sends to the sender; and the page's stored row in the module's SQLite database (`plugins/UltiTools/sqliteDB/UltiRemoteBag.db`, table `remote_bags`). Before step (a), read the sender's inventory once and attach it: it must show the pane at `0b` with count 1, the `DIAMOND` at `1b` with count 1, and nothing else, or the row is `blocked` before it starts. Record the pane's printed components as evidence and assert no sub-field of them: the verdicts below rest on item counts, which do not depend on how this build prints a component | Inside the open page, in this order. (a) Left-click the renamed pane onto the cursor and double-click content slot 1; then left-click an empty slot of your own inventory to put down whatever the cursor now holds, and read `minecraft:data get entity <player> Inventory`. (b) Shift-click that pane from your own inventory towards the page, and read the inventory again. (c) As a control, left-click the `DIAMOND` from your own inventory onto the cursor and double-click content slot 1; then put the carried stack down into an empty slot of your own inventory and read the inventory again. (d) Close the page with Escape (an edit-mode close saves content slots 0-44) and read the stored row: `sqlite3 plugins/UltiTools/sqliteDB/UltiRemoteBag.db "SELECT contents LIKE '%glass_pane%', contents LIKE '%diamond%' FROM remote_bags WHERE player_uuid='<uuid>' AND page_number=<page>"`. Note the chat after each of (a), (b) and (c) | (a) and (b) are refused and (c) is not. After (a) the red `That item matches one of this page's buttons, so the action was cancelled — it would have taken the button out of the toolbar or merged your item into it.` line is printed, and the inventory read shows exactly one `minecraft:gray_stained_glass_pane` entry, with count 1. That count is the verdict: the five filler icons (raw 46, 47, 48, 51, 52) are the only items in the window similar to the fixture, so a collect that reached the toolbar would have merged them into the carried stack and the count would be above 1. The red line itself is emitted at exactly one of `onClick`'s four exits, the `wouldReachAToolbarIcon` branch, so (a) refused with it also proves the fixture matched a toolbar icon. After (b) the same red line is printed again and the pane is still in the sender's own inventory with count 1: a shift-click let through would have merged it into a filler slot and taken it out of the inventory. After (c) no red line is printed and the inventory shows exactly one `minecraft:diamond` entry, with count 2: vanilla's collect swept the window and took the page's own `DIAMOND` out of content slot 0, because a `DIAMOND` is `isSimilar` to no toolbar icon, so `wouldReachAToolbarIcon` returns false and the click is allowed. After (d) the query prints `0\|0`: the page stores neither the pane nor a diamond. Read (a) against (c): the same action at the same raw slot, seconds apart, is refused for the pane and allowed for the diamond, so the refusal tracks the item and not the action — (c) being allowed is what makes (a) being refused mean something. **These counts are derived from vanilla's collect and shift-click rules and have not yet been measured on a real server, so `pass` requires every value named above at once.** Record `fail` if the pane's count after (a) is above 1 (the collect reached the toolbar), if (b) prints no red line and the pane leaves the inventory (the shift-click reached a filler slot), or if (c) prints the red line (the guard refused an item that matches no button). If (a) prints no red line and the pane's count is exactly 1, the fixture never matched the fillers and the row has not tested the guard: record `blocked`. Any other outcome records `blocked` with every inventory read and the query output attached, never `pass`. Examples are a pane count of 0 after (a) (the pane stayed in the page, and this row cannot tell the content area from the toolbar at that point), a diamond count other than 2 after (c), and a query result other than `0\|0`. Before `UltiKits/UltiRemoteBag#27`, (a) collected the filler icons onto the cursor as pure duplication (reopening the page rebuilds the toolbar), and (b) merged the pane into a bottom-row slot that `saveCurrentContents` never serialises, losing it on close | server | RemoteBagContentGUI#onClick |
| ultiremotebag.bag.save | `language: en`; sender owns at least one bag page, NOT currently open in any GUI, and that page is in the service cache — run `/bag <page>` once and close it. Do NOT run this from a fresh login: nothing has populated the cache then (`loadBagIfNeeded` is reached only from the open/list/admin/create paths, and this module's listener handles only `PlayerQuitEvent`), so the command writes no row at all and the Expected below would be false against correct code | Read the page's current `last_updated` directly from the module's own SQLite database (`plugins/UltiTools/sqliteDB/UltiRemoteBag.db`, table `remote_bags`, e.g. `sqlite3 <path> "SELECT last_updated FROM remote_bags WHERE player_uuid='<uuid>' AND page_number=<page>"`), run `/bag save`, then read the same value again | Chat line reads `Bag saved manually!` (green, `bag_saved_manually`) and the second read of `last_updated` is later than the first — this is the cached-pages-only path, with no open page to flush; the open-page case is `ultiremotebag.bag.save.open-gui-flushed` below. The success line is now conditional on a write having happened: with nothing cached the command says `Nothing to save.` instead, which is what made the old "or run this from a fresh login" precondition assert against correct code | server | |
| ultiremotebag.bag.save.open-gui-flushed | `language: en`; sender owns at least one bag page, opened via `ultiremotebag.bag.open-page` above, with a NEW distinguishable item (e.g. a single `DIAMOND`) placed into content slot 0, GUI STILL OPEN, Save button NOT yet clicked (do not close the GUI at any point during this row — every close path, including Escape, runs `onClose`'s own `saveCurrentContents()` in edit mode and would prove nothing about this command). The command must be issued AS THE SENDER while the container screen is open, and this row is performed with any mechanism that achieves that. **Two are measured to work, and EITHER satisfies the row:** (i) any plugin calling `Bukkit.dispatchCommand(<the sender>, "bag save")` — `CraftServer#dispatchCommand` builds the command source from the sender it is given and checks no container, so the command arrives as that player; or (ii) a client able to send a `ServerboundChatCommandPacket` with the container screen open — the whole server-side chain (`handleChatCommand`, `tryHandleChat`, `performUnsignedChatCommand`, `Commands#performCommand`) never consults the player's `containerMenu`, so the server accepts it from anything that can send the packet. Both measured from `paper-1.21.11.jar`; the same query finds `containerMenu` in ten other packet handlers, 38 times in `handleContainerClick` alone, so the zero is the code's and not the query's. **One mechanism is measured NOT to work and must not be substituted:** `/execute as <player> run bag save` from the console or a command block — `CommandSourceStack#getBukkitSender()` reads the stack's `source`, and `withEntity` rebuilds the stack with that same `source` while substituting only `entity`, so the sender is still the console, which `@CmdTarget(PLAYER)` refuses. Separately, and as background rather than as a requirement of this row: a tester cannot simply type the command, because a vanilla client does not open the chat screen while a container screen is active. That is why a mechanism is needed at all — it is sourced from the client's documented keybind behaviour, not measured here, and nothing in this row depends on it: were it wrong, the row would simply have gained a third usable mechanism | Issue `/bag save` as the sender, through any mechanism that delivers it as that player while the GUI remains open — the two measured ones are named in the Preconditions — and record which one was used. If no such mechanism is available on the UAT machine, record this row `blocked` with that as the reason rather than closing the GUI, which would test `onClose` instead. Then — without closing the GUI — read the stored contents directly from the module's own SQLite database (`plugins/UltiTools/sqliteDB/UltiRemoteBag.db (per SQLiteDataStore's dbPathForPlugin: a module's default getDataOperator call resolves to <UltiTools core data folder>/sqliteDB/<pluginName>.db, not a per-plugin subfolder)`, table `remote_bags`, e.g. `sqlite3 <path> "SELECT contents FROM remote_bags WHERE player_uuid='<uuid>' AND page_number=<page>"`), bypassing the plugin's in-memory cache entirely — any subsequent in-game `/bag` command would instead reuse that same cache and could not distinguish this case | The stored `contents` column DOES include the `DIAMOND` (`id: minecraft:diamond` under `items.'0'`), and the GUI is still open. The database read is the verdict, not the `Bag saved manually!` line, which this command printed even while storing nothing before `UltiKits/UltiRemoteBag#22`. The flush branch is NOT dead code that only a mod can reach: a plugin dispatching `bag save` on a player's behalf from a scheduled task, a region trigger or a command alias is an ordinary production trigger and needs no client modification — it is only the tester who needs a mechanism to reproduce it by hand | server | |
| ultiremotebag.bag.persistence | `language: en`; a distinguishable item (e.g. a single `DIAMOND`) placed into slot 0 of an owned bag page, then persisted via the content GUI's OWN Save button (`/bag save` with the page still open also persists it as of `UltiKits/UltiRemoteBag#22`, covered by `ultiremotebag.bag.save.open-gui-flushed` above; this row deliberately uses the Save button so it tests only the persistence layer) | Click the content GUI's Save button, then restart the server completely (not `/ul reload`), then log back in and run `/bag <page>` for the same page | The `DIAMOND` is present in slot 0, exactly as left before the restart — `RemoteBagContentGUI#saveCurrentContents` copied the GUI's live inventory into the service cache and `RemoteBagService#loadBagIfNeeded` re-queried `remote_bags`, deserializing the stored YAML back into the same slot | server | |
| ultiremotebag.bag.cleanup-on-quit | `language: en`; the sender's page 1 already has a saved row in `remote_bags` containing a known item (e.g. a `DIAMOND` in slot 0 — click the content GUI's Save button once, per `ultiremotebag.bag.persistence` above, then close the GUI so no lock/GUI state is open going into this row) | While the player is OFFLINE (having quit normally), an admin edits the `contents` column of that same row DIRECTLY in the module's own SQLite database (`plugins/UltiTools/sqliteDB/UltiRemoteBag.db (per SQLiteDataStore's dbPathForPlugin: a module's default getDataOperator call resolves to <UltiTools core data folder>/sqliteDB/<pluginName>.db, not a per-plugin subfolder)`) to a recognizably different serialized state (e.g. swap the item for an `EMERALD`); the player then logs back in and runs `/bag 1` again | The player sees the ADMIN-modified content (the `EMERALD`), not the pre-quit in-memory state — proving `bagCache` was actually evicted for this player by `BagListener#onPlayerQuit`'s `RemoteBagService#clearCache` call specifically. This isolates the listener's own effect from `RemoteBagContentGUI#onClose`'s lock-release, which runs on every GUI close (including the one triggered by quitting) but does NOT clear the service cache — only the quit listener does that, so a stale un-evicted cache would instead still show the pre-quit `DIAMOND` | server | |
| ultiremotebag.bag.lock-not-persisted | none | Read the pinned copy `checklist-copy/src@b30a910/BagLockService.java` (SHA-256 `9bad43143bc32658bf7a5a99200bdaf470b45a67081b5ec0bd4f6c12558ba95d`), not the live source tree, per D-11 (a structural assertion about the absence of a persistence mechanism, which has no runtime observable short of a genuine non-clean process kill against the shared server — out of this checklist's scope): `private final Map<String, BagLockInfo> locks = new ConcurrentHashMap<>();` is a plain runtime field with no `@Table`, no `DataOperator`, and no file I/O anywhere in this class (grep the pinned copy for each of `@Table`, `DataOperator`, and `getOperator` — each returns 0 hits) | No persistence mechanism exists for `locks` at all — every held lock is silently discarded on any non-clean shutdown (crash, kill -9) and reconstructed empty on the next boot, unlike a clean quit, which explicitly releases the quitting player's locks via `ultiremotebag.bag.cleanup-on-quit` before the map would otherwise be discarded anyway | protocol | |

## Admin

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiremotebag.admin.clear | `language: en`; admin holds `ultibag.use` AND `ultibag.admin.clear`; target player has an unlocked bag page containing at least one item | Run `/bag clear <target> <page>` | Chat line reads `Cleared bag #<page> of player <target>` (green, `admin_bag_cleared`); the target's bag page, when next opened, shows an empty content area (items removed, page itself still exists) | server | |
| ultiremotebag.admin.clear.neg-locked | `language: en`; admin holds `ultibag.use` AND `ultibag.admin.clear`; the target player currently holds the OWNER edit lock on the page (target has it open in edit mode) | Run `/bag clear <target> <page>` | Chat line reads `Bag is in use, cannot clear` (red, `bag_in_use_cannot_clear`) — `BagLockService#canUpgradeToEdit` refused because the owner's lock has not expired; the page's contents are UNCHANGED | server | |
| ultiremotebag.admin.create | `language: en`; admin holds `ultibag.use` AND `ultibag.admin.create`; target player has played before | Run `/bag create <target>` | Chat line reads `Created bag #<N> for player <target>` (green, `admin_bag_created`), where `<N>` is one past the target's highest existing page number; `/bag list <target>` immediately afterward shows the new page | server | |
| ultiremotebag.admin.create.neg-never-played | `language: en`; admin holds `ultibag.use` AND `ultibag.admin.create`; `<name>` has never played on this server | Run `/bag create <name>` | Chat line reads `Player not found: <name>` (red, `player_not_found`) | server | |
| ultiremotebag.admin.delete | `language: en`; admin holds `ultibag.use` AND `ultibag.admin.delete`; target player has AT LEAST TWO unlocked bag pages (run `ultiremotebag.admin.create` above first, so the target has page 1 plus a second page), deleting the SECOND (non-1) page — deleting the target's only page (page 1) would not demonstrate removal at all, since `getPlayerBagPages`'s empty-cache fallback re-fabricates a virtual page 1 immediately afterward (`UltiKits/UltiRemoteBag#26`) | Run `/bag delete <target> <page>` naming the SECOND page number | Chat line reads `Deleted bag #<page> of player <target>` (green, `admin_bag_deleted`); `/bag list <target>` immediately afterward lists only page 1 (the second page is gone), and the deleted page's `remote_bags` row is gone | server | |
| ultiremotebag.admin.delete.neg-locked | `language: en`; admin holds `ultibag.use` AND `ultibag.admin.delete`; the target player currently holds the OWNER edit lock on the page | Run `/bag delete <target> <page>` | Chat line reads `Bag is in use, cannot delete` (red, `bag_in_use_cannot_delete`); the page still exists afterward | server | |
| ultiremotebag.admin.list | `language: en`; admin holds `ultibag.use` AND `ultibag.admin.list`; target player owns at least one bag page with a known item/slot count | Run `/bag list <target>` | Chat starts with `=== <target> 's Bag List ===` (gold), then one line per page reading `  #<N> - <itemCount> items, <stackCount> slots` (yellow `#N`, white separator), then ends with `Total <count> bags` (gold) | server | |
| ultiremotebag.admin.list.neg-no-bags | `language: en`; admin holds `ultibag.use` AND `ultibag.admin.list`; target player has played but every one of their bag pages has been deleted via `ultiremotebag.admin.delete` first | Run `/bag list <target>` | The `No bags`/`Total 0 bags` empty-state branch is UNREACHABLE — `RemoteBagService#getPlayerBagPages` fabricates a virtual page 1 whenever the cache holds no real pages, so the output instead shows exactly one page (`#1 - 0 items, 0 slots`) and `Total 1 bags`, never the empty-state text. Known product defect, `UltiKits/UltiRemoteBag#26` | server | |
| ultiremotebag.admin.see | `language: en`; admin holds `ultibag.use` AND `ultibag.admin.see`; target player owns at least one bag page, currently NOT locked by anyone | Run `/bag see <target>` | `RemoteBagContentGUI` opens for the target's lowest-numbered page in EDIT mode (the admin acquires an ADMIN lock, since none existed) — same layout as `ultiremotebag.bag.content-gui` | pixel | RemoteBagContentGUI |
| ultiremotebag.admin.see-page | `language: en`; admin holds `ultibag.use` AND `ultibag.admin.see`; target player owns the specific page requested | Run `/bag see <target> <page>` | `RemoteBagContentGUI` opens for that exact page (same lock-acquisition rule as `ultiremotebag.admin.see`) | pixel | RemoteBagContentGUI |
| ultiremotebag.admin.see-page.neg-owner-blocked-by-admin | `language: en`; admin holds `ultibag.use` AND `ultibag.admin.see`; a SECOND admin already holds the ADMIN edit lock on the target's page (that second admin ran `ultiremotebag.admin.see-page` first and left the GUI open) | As the FIRST admin, run `/bag see <target> <page>` | The message is the actual (Simplified Chinese) text `BagOpenResult.blocked` builds for `LockType.ADMIN` — in English, "This bag is being edited by admin `<name>`, please try again later"; no GUI opens. A known product defect: this refusal text ignores the server's `language` setting entirely, `UltiKits/UltiRemoteBag#20` | server | |
| ultiremotebag.admin.see.neg-never-played | `language: en`; admin holds `ultibag.use` AND `ultibag.admin.see`; `<name>` has never played on this server | Run `/bag see <name>` | Chat line reads `Player not found: <name>` (red, `player_not_found`) | server | |
| ultiremotebag.admin.see.neg-no-bags | `language: en`; admin holds `ultibag.use` AND `ultibag.admin.see`; target has played but every page deleted first via `ultiremotebag.admin.delete` | Run `/bag see <target>` | The `player_no_bags` refusal is UNREACHABLE for the same reason as `ultiremotebag.admin.list.neg-no-bags` — `RemoteBagContentGUI` instead opens for the fabricated virtual page 1 in EDIT mode. Known product defect, `UltiKits/UltiRemoteBag#26` | pixel | RemoteBagContentGUI |
| ultiremotebag.admin.see.neg-readonly-then-owner-blocked | `language: en`; admin holds `ultibag.use` AND `ultibag.admin.see`; the target player (owner) currently holds the OWNER lock on the page (the owner runs `ultiremotebag.bag.open-page` above and leaves the GUI open) with exactly one `DIAMOND` stored in content slot 0 (place it per `ultiremotebag.bag.content-gui.edit-item-movement` above); the admin's own inventory holds a stack of EXACTLY TWO `EMERALD`s on hotbar slot 3 and nothing else (the stack size is load-bearing for step (d): a one-item gesture is not a drag on a real server, measured in `AbstractContainerMenu#doClick` — the second slot is never registered and the gesture is re-dispatched as a `PICKUP` click with no `InventoryDragEvent` constructed, so with one emerald (d) would pass whatever `onDrag` returned) | As the admin, run `/bag see <target>` while the owner still has the page open, then attempt each of the following against the resulting read-only GUI, reading the admin's own entity inventory from the server console after EACH one (`minecraft:data get entity <admin> Inventory`) instead of judging by the chat line: (a) left-click content slot 0; (b) shift-click content slot 0; (c) hover content slot 0 and press the number key for hotbar slot 3; (d) left-click the two-`EMERALD` stack onto the cursor, left-drag across content slots 0 and 1 — press and hold the left mouse button, move the pointer over both slots, release — and then left-click hotbar slot 3 to put the stack back, because a refused drag leaves both emeralds ON THE CURSOR and (e) needs them in the inventory again; (e) shift-click the two-`EMERALD` stack in hotbar slot 3 towards the bag; (f) left-click the disabled save icon in slot 49 | After every one of (a)-(f): the console read of the admin's inventory still shows exactly the TWO `EMERALD`s it started with, as one stack on hotbar slot 3, and no `DIAMOND`; the admin's cursor is empty (the deliberate pick-up inside (d) is undone by that step's own final left-click, so the cursor is empty again by the time the step is judged); the bag's content slot 0 still holds the `DIAMOND`; no content slot holds the `EMERALD`; and slot 49 still holds the red stained-glass `Save (Disabled)` icon. The inventory read is the verdict for every one of (a)-(f) — the defect this row exists to catch printed the correct refusal line while the item really moved into the admin's inventory, so a chat line alone proves nothing was cancelled (`UltiKits/UltiRemoteBag#27`). Chat, as a secondary observation only: (a), (b), (c), (d) and (e) each print a red `Read-only mode, cannot move items` line and play `sound.error` — (d) included, because a refused drag now says why as well (an earlier revision of this row said (d) was silent, which was wrong twice over: the drag path announces its refusals, and a one-item gesture degenerates into a click, which announced itself already); (f) prints the red `Currently in read-only mode, cannot save changes` line and saves nothing. Two lines are shown when the GUI opens: first the (Simplified Chinese) text `BagOpenResult.readOnlyMode` builds, meaning "This bag is being used by `<owner>`, currently read-only" — a known product defect that this text ignores `language`, `UltiKits/UltiRemoteBag#20` — then a yellow `The owner has this page open right now, so the lock timeout does not hand it over. Click refresh once they close it.`, which does honour `language`. Separately, if the owner then closes their GUI and the admin clicks the refresh button, the admin's view upgrades to edit mode and a green chat line reads "Owner has exited, switching to edit mode..." | pixel | RemoteBagContentGUI |
| ultiremotebag.admin.see.neg-readonly-notify-disabled | `language: en`; two accounts, the owner and an admin who is NOT OP and holds `ultibag.use` AND `ultibag.admin.see` (no step needs either test player to be OP: `/bag see` is gated by those two permission nodes only); the owner owns page 1; `lock.notify_readonly_viewers: false` in `config/remotebag.yml`, applied with `ul reload UltiRemoteBag` run from the server console, with no restart (`/ul` is `requireOp`, and the console satisfies that without making either test player OP; this key is read on every notification, and the reload is part of what this row proves) | 1. The owner runs `/bag 1` and leaves the page OPEN. 2. The admin runs `/bag see <owner>` and leaves the resulting read-only page OPEN. 3. The owner presses Escape to close their page. 4. The owner runs `/bag 1` again. 5. Read the admin's chat. 6. The owner and the admin both close their pages with Escape: both are still open, from steps 4 and 2. Set `lock.notify_readonly_viewers: true`, run `ul reload UltiRemoteBag` from the server console, and repeat steps 1-5 with the same two accounts. Closing first is not optional for a scripted client, which can send `/bag 1` over a page that is still open: that re-open drops the page's lock (`UltiKits/UltiRemoteBag#41`), so the admin's `/bag see` would get edit mode, the owner would be blocked at step 4, and the second half would fail against a correct build. | At step 5 the admin's chat carries NO line containing `has started using this bag`. At step 6's step 5 it carries `<owner> has started using this bag, your view has been switched to read-only mode. Click the refresh button to try to acquire edit permission.` (yellow, `msg_owner_started_using`). Step 6 is not optional: without it, step 5's silence cannot be told apart from a notification that never fires at all, which is the state this key was in before `UltiKits/UltiRemoteBag#19`. Because both halves are applied with a console `ul reload` and no restart, the pair also proves the value is read live rather than copied into a field at boot | server | BagLockService#notifyReadOnlyAdmins |
| ultiremotebag.admin.see.neg-owner-present-outlives-lock-timeout | `language: en`; `lock.timeout_seconds: 10` in `config/remotebag.yml` and the server restarted so the value is in force (this key is read once at boot, so `/ul reload` will not apply it); the owner is any online player holding `ultibag.use` who owns page 1 with exactly one `DIAMOND` stored in content slot 0; the administrator is a different online player holding both `ultibag.use` and `ultibag.admin.see`, whose own inventory holds exactly one `EMERALD` on hotbar slot 0 and nothing else | The owner runs `/bag 1`, leaves the page open and performs NO further action for at least 15 seconds — no clicks, no movement, nothing that could be read as activity. The administrator then runs `/bag see <owner> 1`, and against the resulting page: (a) left-clicks the `EMERALD` in their own inventory onto the cursor and left-clicks content slot 5 to place it; (b) left-clicks the save icon in slot 49; (c) left-clicks hotbar slot 0 to put the `EMERALD` back into their own inventory — the refusal in (a) leaves it on the CURSOR, and `minecraft:data get entity <player> Inventory` does not report a cursor item, so without this step the final read would show no emerald anywhere and the row would fail against a correct build. The owner then presses Escape to close their page. Finally read BOTH inventories from the server console (`minecraft:data get entity <player> Inventory`) and the stored row directly from the module's own SQLite database (`plugins/UltiTools/sqliteDB/UltiRemoteBag.db`, table `remote_bags`, e.g. `sqlite3 <path> "SELECT contents FROM remote_bags WHERE player_uuid='<owner uuid>' AND page_number=1"`) | The administrator's page opens in READ-ONLY mode even though the lock is older than `lock.timeout_seconds`, because the owner still has the page open: the timeout reclaims a lock whose holder's session ended, not one whose holder is present, however long they idle. Two chat lines appear for the administrator — the read-only notice `BagOpenResult.readOnlyMode` builds (Simplified Chinese whatever `language` holds, the known product defect `UltiKits/UltiRemoteBag#20`), followed by a yellow `The owner has this page open right now, so the lock timeout does not hand it over. Click refresh once they close it.` — the second is the point of this row: a silent downgrade would be indistinguishable from a bug. Then (a) is refused with a red `Read-only mode, cannot move items` line and (b) with a red `Currently in read-only mode, cannot save changes` line. The verdict is the item count, not the chat: after the owner's close, the console read shows the `EMERALD` still in the administrator's own inventory, the stored `contents` column still holds the `DIAMOND` under `items.'0'` and does NOT mention `minecraft:emerald`, and no item exists twice. Before this change the administrator's page opened in EDIT mode, their `EMERALD` was written into the row by (b), and the owner's close then wrote their pre-administrator snapshot over it — destroying the `EMERALD` outright | server | BagLockService#isReclaimable, RemoteBagContentGUI#isPageOpenBy |
| ultiremotebag.admin.see.edit-and-save-with-no-lock-held | `language: en`; same two players and same starting items as `ultiremotebag.admin.see.neg-owner-present-outlives-lock-timeout` above; no `lock.timeout_seconds` change is needed and none is made — see the Expected for why | The owner runs `/bag 1` and then CLOSES the page (Escape). The administrator then runs `/bag see <owner> 1`, places their `EMERALD` into content slot 5, left-clicks the save icon in slot 49 and closes the page; read the stored row from the module's own SQLite database as in the row above | The administrator's page opens in EDIT mode and the stored `contents` column afterwards contains BOTH `minecraft:diamond` and `minecraft:emerald`. This is the control for the row above, and it establishes exactly one thing: an administrator who is NOT contending with a present owner gets edit mode and their save reaches the database — so that row's "nothing moved" verdict cannot come from a page that was never editable for some unrelated reason. It deliberately does NOT exercise the `lock.timeout_seconds` reclaim path, and an earlier revision of this row claimed it did, which was wrong: closing the page runs `RemoteBagContentGUI#onClose`, which releases the lock immediately, so the administrator takes the no-lock path whatever the timeout is set to and however long the tester waits — the row would have passed even if expired locks were never reclaimed at all. That path is not reachable at a real server either, and deliberately so: every clean departure releases the lock (closing the page, and `PlayerQuitEvent` via `BagListener`), and the lock map is memory-only, so a non-clean kill discards it wholesale rather than leaving an expired entry behind — see `ultiremotebag.bag.lock-not-persisted`. The reclaim branch is therefore covered by unit tests only, named here so the gap is visible: `OwnerPresenceLockHandoverTest#anOfflineHoldersLockIsReclaimed` and `OwnerPresenceLockHandoverTest#aLockOnAPageNobodyIsLookingAtStillExpires` | server | BagLockService#adminOpen |

## Lifecycle Hooks

These rows exercise `UltiKits/UltiRemoteBag#12`'s wave-0 lifecycle-hook migration: `unregisterSelf()`
and `reloadSelf()` are now `final` framework template methods. This module's bag save moved verbatim
into the `onUnregister()` hook, and its log-only reload override was deleted, so `/ul reload` now runs
the framework's own configuration reload for this module. Every cache-write path in this module already
saves synchronously (`UltiKits/UltiRemoteBag#23`), so "put an item in a bag, unload, reinstall, see the
item" would pass whether or not the hook ran; the unload row instead makes the database differ from the
cache just before the unload, and reads the row back before restoring anything. It used to need a
timing dance to tell the hook's write apart from the scheduled auto-save's; `UltiKits/UltiRemoteBag#13`
removed that task, so the hook is now the only writer of an idle player's row and the row says so
directly.

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiremotebag.lifecycle.reload | `language: en`; in `plugins/UltiTools/pluginConfig/UltiRemoteBag/config/remotebag.yml`, `permission_based_pages: false` and `max_pages: 1`, both set BEFORE a full server restart so they are the values loaded at boot; sender holds `ultibag.use` | 1. Run `/bag 2`. 2. Without restarting, edit the same file to `max_pages: 3` and save it. 3. In the server console run `ul reload UltiRemoteBag`. 4. Run `/bag 4`. 5. Cleanup: set `max_pages` back to its previous value and run `ul reload UltiRemoteBag` again. | Step 1: chat line reads `Page 2 out of range, max page is 1` (red, as in `ultiremotebag.bag.open-page.neg-out-of-range` above). Step 3: the console shows the framework line `[com.ultikits.ultitools.abstracts.UltiToolsPlugin] Module 'UltiRemoteBag' reloaded.`, NO `UltiRemoteBag configuration reloaded!` line appears, and the console receives `Module UltiRemoteBag has been reloaded`. Step 4: chat line reads `Page 4 out of range, max page is 3` — `ConfigManager#reloadConfigs` re-initialised the `RemoteBagConfig` bean that `RemoteBagService#getPlayerMaxPages` reads on every call. If step 4 still reads `max page is 1`, record `fail`: the configuration was not reloaded (the pre-migration behaviour, `UltiKits/UltiRemoteBag#12`) | server | |
| ultiremotebag.lifecycle.removed-key-warning | The server has run this module at least once, so `plugins/UltiTools/pluginConfig/UltiRemoteBag/config/remotebag.yml` exists; the server is STOPPED for each edit below, so the file the module reads at the next boot is unambiguously the one that was edited | 1. With the server stopped, make sure `plugins/UltiTools/pluginConfig/UltiRemoteBag/config/remotebag.yml` contains the line `auto_save_interval: 300` — a server upgraded from 6.2.x already has it, and a file that does not can have it added by hand. 2. Start the server and read the console from the `UltiRemoteBag has been enabled!` line upwards. 3. Stop the server, delete that one line from the same file, start the server again and read the same part of the console. | Step 2 logs exactly one line reading `[UltiTools] [UltiRemoteBag] UltiRemoteBag: 'auto_save_interval' in config/remotebag.yml no longer has any effect and can be deleted from the file -- the periodic auto-save it named was removed because it had nothing to do; every write to a bag page is already persisted in the same action (UltiKits/UltiRemoteBag#13, UltiKits/UltiRemoteBag#23).` Step 3 logs no such line at all. Step 3 is the control and is not optional: a check that never fires and a file with no residual key produce the same empty log, so step 2 on its own cannot tell them apart | server | |
| ultiremotebag.lifecycle.unload | `datasource.type: sqlite` in `plugins/UltiTools/config.yml` (shipped default); the `sqlite3` CLI available on the server host, whose local time zone is the one the server console timestamps use; the player's page 1 already has a saved row in `remote_bags` whose contents hold a single `DIAMOND` in slot 0 (place it and click the content GUI's own Save button, per `ultiremotebag.bag.persistence` above, then close the GUI); the player then stays ONLINE, with no bag GUI open, for the whole row — a quit would save and evict the cache through `ultiremotebag.bag.cleanup-on-quit` instead of this hook; a copy of the installed module jar is kept OUTSIDE `plugins/UltiTools/plugins/`, because `/upm uninstall` deletes the jar it unloads | 1. Empty the stored page while it stays cached: `sqlite3 plugins/UltiTools/sqliteDB/UltiRemoteBag.db "UPDATE remote_bags SET contents='', last_updated=0 WHERE player_uuid='<uuid>' AND page_number=1"`. 2. Read it back with `sqlite3 plugins/UltiTools/sqliteDB/UltiRemoteBag.db "SELECT contents LIKE '%diamond%', last_updated, strftime('%H:%M:%S', last_updated/1000, 'unixepoch', 'localtime') FROM remote_bags WHERE player_uuid='<uuid>' AND page_number=1"`; the output must begin `0\|0\|` (the third field is just epoch 0 rendered in local time). 3. In the server console run `upm uninstall UltiRemoteBag` (`UltiRemoteBag` is the runtime name: `PluginInstallUtils#uninstallPlugin` matches `UltiToolsPlugin#getPluginName`, which is this module's `plugin.yml` `name`), and note the console timestamp of the `UltiRemoteBag has been disabled!` line. 4. Before restoring anything, run step 2's query again. 5. Cleanup: copy the kept jar back into `plugins/UltiTools/plugins/`, restart the server, log the same player in, and run `/bag 1`. | Step 3: the console logs `[UltiTools] [UltiRemoteBag] UltiRemoteBag has been disabled!`. Step 4 prints `1\|<L>\|<HH:MM:SS>`, where `<HH:MM:SS>` equals the disabled line's console timestamp or is exactly one second before it. Together these prove the hook's save wrote the row: `RemoteBagService#saveBag` stamps `last_updated` immediately before its update, and `onUnregister()` saves synchronously just before logging the disabled line. No other writer can account for it — the player stays online so the quit listener does not run, and `UltiKits/UltiRemoteBag#13` removed the scheduled auto-save that used to rewrite an idle player's row every 300 seconds (which is why this row no longer has to find that task's phase first). In step 5 the `DIAMOND` is in slot 0; step 5 alone proves nothing about the hook, because it only shows what step 4 already read. Record `fail` if step 4 prints `0\|0\|...` (nothing saved the cache on unload) | server | |

## Configuration

One row per `@ConfigEntity` class (D-06's config-per-file rule), not per key: `RemoteBagConfig`
(`config/remotebag.yml`, 17 keys), matching `FEATURES.md`'s `## Configuration` section exactly.
This row confirms every key is present at its documented default, then flips one representative
interval-style key and observes the behaviour follow. Six of the seven keys `FEATURES.md` used to
document as having no observable effect (`auto_save_interval`, `gui_title`, `messages.no_permission`,
`messages.page_locked`, `messages.bag_saved`, `save_on_close`) were removed by
`UltiKits/UltiRemoteBag#13`, `#14`, `#15`, `#16`, `#17`, `#18` and `#23`, and so was `rows_per_page`,
which did have an effect but not the one it declared (`UltiKits/UltiRemoteBag#24`); the row below now checks for their ABSENCE, and the warning an
operator gets when one is still in their file is exercised by
`ultiremotebag.lifecycle.removed-key-warning` above. Of the remaining two, `save_on_close` was removed as well
(`UltiKits/UltiRemoteBag#18`, reversed from a wiring on 2026-09-23 because the branch it switched off
destroyed the window's contents), and `lock.notify_readonly_viewers` was wired by
`UltiKits/UltiRemoteBag#19` and has its own row in the `## Admin` section above. No key below is
exempt from being exercised under the "do not test a key with no effect" convention the framework's
own `UAT-CHECKLIST.md` applies to `ultipanel.logging.batch.interval`, because none is in that state
any more.

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultiremotebag.config.remotebag-yml | Fresh `config/remotebag.yml` at its shipped default | Load the file; take the 24 keys listed under the pinned copy `checklist-copy/FEATURES.md@b30a910.md`'s `## Configuration` section (SHA-256 `deb72b62a5238af6fee03dbbe2006ea80521cbc2d1e426c186b0deb90eb5618c`, per D-11 — a source-derived documented-defaults cross-check, not a live command output, and deliberately still the pre-6.3.0 list, because a pinned fixture records what it recorded and is not rewritten), subtract the seven removed by `UltiKits/UltiRemoteBag#13`, `#14`, `#15`, `#16`, `#17`, `#18` and `#24` (`auto_save_interval`, `gui_title`, `messages.no_permission`, `messages.page_locked`, `messages.bag_saved`, `save_on_close`, `rows_per_page`), and confirm the remaining 17 are present at the pinned copy's documented defaults and that none of the seven appears at all. Use that pinned list rather than the live `FEATURES.md` on disk, which could have drifted from the commit this row's fixture was pinned against. The two `lock.` keys are exercised by their own rows rather than here, per the config-per-file rule: `lock.timeout_seconds` by `ultiremotebag.admin.see.neg-owner-present-outlives-lock-timeout` above, which sets it to 10 and restarts, and `lock.notify_readonly_viewers` by `ultiremotebag.admin.see.neg-readonly-notify-disabled` above. Do not add a timeout step here: a lowered timeout has no effect a real server can show. A lock is never reclaimed while its holder has the page open, and every clean departure releases the lock outright, so the reclaim path cannot be reached at a real server (see `ultiremotebag.admin.see.edit-and-save-with-no-lock-held` above). That the configured value reaches `BagLockService#setLockTimeout` at boot is covered only by the unit test `UltiRemoteBagTest.RegisterSelf#setsLockTimeout` | The 17 remaining keys are present at the pinned copy's documented defaults, and none of the seven removed keys is present | server |  |
